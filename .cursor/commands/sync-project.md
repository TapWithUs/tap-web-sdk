# sync-project

This project has to mirror the capabilities in the python sdk, so please sync the project with the python sdk. If there is nothing to sync - do nothing. When syncing, compare the state of the project branch with the python sdk branch names.
You may clone the python sdk repo to a local 'python-sdk-tmp' directory and operate on it. Make sure to delete the directory after the sync is complete.